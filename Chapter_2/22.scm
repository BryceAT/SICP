#|
Louis Reasoner is building the answer from the back to the front but taking arguments from the front of items.  This reverses the list.

the second idea does not work because it in scheme a list has the pointer in the second component. so lists this way are implemented as a stack with the first in last out policy.
|#
