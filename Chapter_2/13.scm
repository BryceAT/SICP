#|
(load "Desktop/scheme_script/Chapter_2/13.scm")
|#
assume both intervals are positive
(c1(1-p1),c1(1+p1)) * (c2(1-p2),c2(1+p2)) = (c1c2(1-p1)(1-p2),c1c2(1+p1)(1+p2))
p1 + p2 + p1p2
p1p2 - p1 - p2
so if p1 and p2 are much closer to 0 than 1 then the product is on a smaller scall than either p1 or p2
so the approximation is p1 + p2.
